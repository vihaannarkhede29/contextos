---
description: ContextOS project memory — architecture, conventions, and decisions
alwaysApply: true
---

# ContextOS Project Memory

> Auto-generated by ContextOS. Files indexed: 76. Last indexed: 2026-06-26T01:46:59.889Z.

## Frameworks

- **Next.js**: Uses Next.js
- **FastAPI**: Uses FastAPI
- **PostgreSQL**: Uses PostgreSQL
- **Prisma**: Uses Prisma
- **shadcn/ui**: Uses shadcn/ui
- **Vitest**: Uses Vitest
- **React**: Uses React
- **TailwindCSS**: Uses TailwindCSS
- **Framer Motion**: Uses Framer Motion

## Conventions

- Colocated Tests: Tests are colocated with source files.

## Important Modules

- **UI Components**: Shared UI components (shadcn/ui): apps/dashboard

## Project Overview

# Project Overview

Framework: Next.js

## Architecture Rules

* Uses Next.js
* Uses FastAPI
* Uses PostgreSQL
* Uses Prisma
* Uses shadcn/ui
* Uses Vitest
* Uses React
* Uses TailwindCSS
* Uses Framer Motion
* Tests are colocated with source files.

## Important modules

* UI Components: Shared UI components (shadcn/ui): apps/dashboard

## Indexed Files

Total: 76 files
